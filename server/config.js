/*
var array = new Uint32Array(10);
window.crypto.getRandomValues(array);
//this generates better random numbers for the offset for each token
*/

module.exports = {
    'secret': '<secret>',
    'expiry_time': 86400 //24hrs
}